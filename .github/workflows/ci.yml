name: Java CI

on:
  push:
    branches:
      - 'feature/**'

  pull_request:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: prj

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build project
        run: mvn clean package -DskipTests

  docker-tests:
    runs-on: ubuntu-latest
    needs: build

    env:
      POSTGRES_DB_TEST: testdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      SPRING_DATASOURCE_URL_TEST: jdbc:postgresql://postgres:5432/testdb
      JWT_SECRET: testsecret

    defaults:
      run:
        working-directory: prj

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run docker integration tests
        run: |
          docker compose -p testenv \
            -f docker-compose.test.yml \
            up --build \
            --abort-on-container-exit \
            --exit-code-from app-test
